# AI 修改记录

## 2026-06-03

- 根据 `README.md` 的最新分工同步项目级 `AGENTS.md`：成员A负责框架、前端、后端路由与接口调度；成员B负责数据读取、导出与清洗；成员C负责可视化；成员D负责数据分析与机器学习。
- 更新分工边界说明，明确成员A维护框架与接口调度时只预留成员B/C/D接口，不实现其具体业务逻辑。

## 2026-06-04

- 去除处理流程页预测分析模块中按钮下方冗余展示的 JSON 示例块，新增“预测结果”显示区域，并让前端预测接口返回内容写入该区域。
- 预测结果区域补充“仅展示摘要和前 20 条”的说明；将预测填充按钮改为读取当前已分析文件的完整可预测数据，并在导出分析结果时同时下载预测结果 JSON。
- 修复 SQLite 状态存储将 `None` 序列化为非空 BLOB 导致清洗、分析、预测结果状态误判的问题；旧库中已写入的 pickle None 会自动清理为 SQL NULL，保证“填充文件数据”只在真实分析结果可用时启用。
- 修复预测输入框为空时直接点击“执行预测”仍显示“输入错误”的交互问题：现在会自动读取当前已分析文件的完整可预测数据并继续预测，读取失败时才提示数据未就绪。
- 从公开 GitHub 数据源下载 TMDB 电影数据原始 CSV，并保存为 `data/tmdb_5000_movies_raw.csv`。
- 抽样生成课程演示数据 `data/movies_demo_sample.csv`，保留电影标题、类型、语言、上映日期、热度、预算、收入、时长、评分、投票数、状态和上映年份字段。
- 已用 Flask test client 验证 `movies_demo_sample.csv` 可完成上传、清洗和 K-Means 分析流程。
- 扩展 SQLite 数据状态为上传历史模式，新增 `dataset_runs` 记录每次上传、清洗结果和分析结果。
- 新增 `GET /api/datasets` 与 `POST /api/datasets/<id>/activate`，支持数据管理页展示上传历史并切换当前处理文件。
- 数据管理页新增上传文件、处理后文件列表；处理流程页和图表分析页新增当前文件状态栏和跳转入口。
- 修复上传拖拽识别，支持拖入 CSV / XLS / XLSX 后更新文件信息。
- 前端导出改为真实下载 CSV / JSON，同时通过页面状态提示导出结果。
- 补充上传历史路由测试，当前 `python -m unittest discover -s tests` 共 36 个测试通过。
- 将数据管理页文件展示从大卡片压缩为 DocAI 风格的紧凑文件行，减少单个文件占用空间。
- 优化数据集列表查询，列表接口只读取元数据和状态标记，避免为每条历史记录反序列化完整 DataFrame。
- 清理源码与测试目录下的 Python `__pycache__` 运行缓存；虚拟环境缓存保留不动。
- 新增 `DELETE /api/datasets/<id>` 和文件行删除按钮，可删除上传历史及其清洗、分析结果；当前文件被删除后自动切换到最近记录或清空状态。
- 补充删除数据集测试，当前 `python -m unittest discover -s tests` 共 39 个测试通过。
- 参考 Dairenwen/DocAI 的 `selected-files-panel` 交互，将数据管理页上传区改为多文件待上传队列，支持拖拽、多选、重复过滤、清空和逐个上传。
- 将上传文件与处理后文件区域改为紧凑文件行，处理、图表、导出和删除入口统一放到底部操作栏；状态标记压缩到文件行右下侧。
- 将处理流程页和图表分析页的当前文件提示改为轻量文件行，仅保留文件信息、“移除”和“添加新文件”。
- 收敛数据管理页按钮：上传文件区只保留“处理 / 图表 / 删除”，处理后文件区只保留“图表 / 导出 / 删除”；单个“导出”按钮按结果状态导出分析 JSON 或清洗 CSV。
- 明确数据管理页文件分区职责：上传文件区聚焦原始数据与当前处理对象，处理后文件区聚焦清洗数据和分析结果归档；“处理”按钮提示进入清洗与分析流程。
- 重设处理流程页为“一键处理”交互：用户确认清洗规则和 k 值后只需点击一次“开始处理”，前端会按“数据清洗 -> K-Means 分析”顺序自动执行。
- 处理流程页新增进度观察和结果摘要：展示上传、清洗、分析、导出阶段状态，以及清洗影响、聚类概览和类别分布。
- 移除数据管理、处理流程、图表分析页的调试响应窗口，并清理对应模板、JS 和 CSS 遗留代码；操作结果改为页面内状态提示。
- 新增 `src/components/DataFileUploadPanel.vue` 作为 Vue / Element Plus 参考组件，不接入当前 Flask 原生前端运行时。

## 2026-06-02

- 新增 Flask 最小可运行骨架。
- 新增单页前端流程，包含上传、清洗、分析、可视化、导出入口。
- 为数据读取、清洗、分析接口返回统一 `NOT_IMPLEMENTED` 占位响应。
- 为可视化接口返回 `DATA_NOT_READY`，等待数据模块接入。
- 新增项目级 `AGENTS.md` 与 `docs/project-context.md`。
- `run.py` 支持 `PORT` 与 `FLASK_DEBUG` 环境变量，并关闭 reloader，方便后台启动验证。
- 未实现数据读取、清洗、分析的具体业务逻辑，未引入大型前端框架。
- 采用方案A优化前端呈现：保留原生 HTML/CSS/JS，改为左侧流程工作台与右侧分析面板布局。
- 为上传、清洗、分析、导出步骤新增可见状态反馈，并优化 Plotly 图表区与接口响应区的空状态。
- 参考 Dairenwen/DocAI 的前端结构，将前端改为多页面工作台形态：`/dashboard`、`/documents`、`/workflow`、`/visualization`。
- 新增共享布局 `layout.html`，采用侧边导航与顶部状态栏；未接入登录、鉴权或多用户功能。
- 按用户视角做前端文案减噪：删除侧栏成员分工、顶部技术栈标签、DocAI 参考说明和“调用接口”等开发者口吻，保留轻量状态反馈用于开发期联调。
- 优化图表分析交互：前端只选择图表类型，不再填写 x/y/color 字段；`/api/visualize` 当前只接收 `chart`，并预留 `description` 用于后续自动图表说明。
- 将网页对外品牌改为 `DataFlow`，移除可见页面中的 `DataAI` 与 `AI` 字样；首页改为流程向导式工作台。
- 优化前端交互形象化：上传页新增文件信息反馈，处理页新增下一步提示，图表页改为可点击图表类型卡片，页面状态反馈保持常显但视觉降级。
- 清理历次前端迭代遗留代码：删除旧单页布局、旧首页模块、旧工具栏和移动端适配 CSS；移除移动端 viewport 与 mobile 截图产物，保留桌面工作台形态。
- 统一 API 响应格式为 `status/code/message/data`；新增开发期单用户数据状态模块 `data_store.py`，并让路由按当前分工调用对应工具函数。
- 重写 README，补充本地运行、统一响应格式、数据状态、各接口调用方式和成员接入位置。
- 按最新成员分工统一文档与占位提示：成员A负责框架、前端、后端路由与接口调度；成员B负责数据读取、导出与清洗；成员C负责可视化；成员D负责数据分析与机器学习。
- 补充成员接入契约：上传、清洗、分析、可视化、导出的返回值和异常约定；分析接口增加 `method=kmeans` 校验。
- 明确成员C可视化函数 `create_chart_response(...)` 返回 `(payload, http_status)`，其中 `payload.data.figure` 需兼容 Plotly.js。
- 将数据状态存储从内存字典改为 SQLite，数据库文件为 `instance/dataflow.sqlite3`；路由仍通过 `data_store.py` 的统一函数读写状态。
- 补充运行环境要求，说明 Python、Flask、SQLite、Plotly.js CDN、可选数据处理依赖和运行环境变量；删除仓库中冗余的 `__pycache__` 缓存文件。
