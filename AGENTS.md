# 项目级 AGENTS.md

## 项目执行规则

本项目为 Python 程序设计团队实验“交互式数据分析系统”。项目级规则优先于全局规则，若无冲突则继承全局 `AGENTS.md` 与 `docs/` 规则。

## 当前技术路线

- 后端：Flask
- 前端：HTML、CSS、JavaScript、Fetch API
- 可视化：Plotly / Plotly.js
- 后续数据处理：Pandas、NumPy
- 后续机器学习：scikit-learn
- 数据存储：优先 SQLite

## 分工边界

- 成员A：整体框架、前端界面、后端路由与接口调度。
- 成员B：数据读取、数据导出以及缺失值、重复值、异常值等数据清洗。
- 成员C：图表可视化，负责 Plotly figure 生成与图表说明。
- 成员D：数据分析与机器学习，负责 K-Means 聚类与分析结果解释。

成员A维护框架与接口调度时只能预留成员B/C/D接口，不实现其具体业务逻辑。

## 开发原则

1. 先保证可运行骨架，再逐步接入各成员功能。
2. 保持 API 路径和 JSON 响应格式稳定。
3. 不引入 React、Vue、Django、MySQL、shadcn/ui 或大型 UI 框架。
4. 不做无关重构，不批量改名，不改动与当前任务无关的模块。
5. 页面以课程演示可操作、可解释、易维护为优先。
